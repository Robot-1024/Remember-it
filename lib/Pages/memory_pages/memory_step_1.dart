import 'package:flutter/material.dart';

class MemoryStep1 extends StatelessWidget {
  const MemoryStep1({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('记忆'),
      ),
    );
  }
}
