import 'package:flutter/material.dart';

class MemoryStep2X2 extends StatelessWidget {
  const MemoryStep2X2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('记忆'),
      ),
    );
  }
}
